const multiply = (num1, num2) => {
    return num1 * num2;
};
let resulMultiply = multiply(2, 31);
console.log(resulMultiply);

Math.floor(Math.random() >0);

const mod = (num3, num4)=>{
    return num3 / num4;
};
let resulDivision = mod(12, 3)
console.log(resulDivision);

const max = parseFloat(prompt("20: "))

const largest = Math.max(20);

console.log("The largest number is " + largest);